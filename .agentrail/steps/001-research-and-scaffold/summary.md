Researched tml24c (C Lisp interpreter), cor24-rs (assembler+emulator), and tc24r (C-to-COR24 compiler). Decided on Rust/Yew architecture using cor24-rs as library — pre-compile tml24c to COR24 assembly, assemble+run in browser emulator, UART for Lisp I/O. Created project: Cargo.toml, Trunk.toml, index.html, src/lib.rs, src/main.rs, src/repl.rs (REPL component with eval button, output area, emulator integration), docs/architecture.md, asm/tml24c.s (20K lines). Both cargo check and trunk build succeed.